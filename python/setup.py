from setuptools import setup

setup(
    name='!NAME!',
    version='0.1.0',
    packages=['!NAME!'],
    entry_points={
        'console_scripts': [
            '!NAME! = !NAME!.__main__:main',
        ],
    },
)
